{
  "name": "@story-studio/desktop",
  "version": "0.1.0",
  "private": true,
  "description": "story-studio 桌面壳（Electron）：内嵌 Fastify server + Web 前端 + 内置浏览器（扫榜/网页AI）",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "electron ."
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "electron": "^44.0.0",
    "typescript": "^5.6.3"
  }
}
